---
description: Build the Lingoland NextJS project
---
Run the following commands to install dependencies and build the project for production.

// turbo-all
1. Install dependencies
```bash
& "C:\Program Files\nodejs\npm.cmd" install
```
2. Build the project
```bash
& "C:\Program Files\nodejs\npm.cmd" run build
```
